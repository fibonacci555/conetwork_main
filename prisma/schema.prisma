datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Knowledge {
  id        Int      @id @default(autoincrement())
  userId    String   @unique
  knowledges String[] @default([])
}